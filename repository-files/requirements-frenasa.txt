# iLuminara-Core FRENASA Engine Requirements
# Voice-to-JSON transformation with offline capabilities

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1
speechrecognition==3.10.1
pyaudio==0.2.14
wave==0.0.2

# ML Models (synchronized with requirements.txt)
torch==2.1.2
transformers==4.36.2

# Swahili NLP
sentencepiece==0.1.99
tokenizers==0.15.0

# Offline Database (IP-05: Golden Thread)
sqlalchemy==2.0.25

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
requests==2.31.0

# Governance Integration
cryptography==41.0.7
filelock>=3.20.3 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
